# Point the frontend at your deployed backend.
# Local dev defaults to http://localhost:8000 if unset.
# On Vercel/Netlify, set this in the project's Environment Variables.
REACT_APP_API_URL=https://vectorshift-backend.onrender.com
