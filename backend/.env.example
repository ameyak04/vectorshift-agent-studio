# Copy to .env for local dev (never commit the real .env).
GEMINI_API_KEY=your-gemini-key
# Optional — enables the real Claude provider; falls back to a mock if unset.
ANTHROPIC_API_KEY=your-anthropic-key
# CORS: "*" for local, or comma-separated origins in production.
ALLOWED_ORIGINS=*
